<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ujamang.biz">
    <!-- 인터넷 권한 설정 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 인터넷 연결 확인 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Codemanager"
        tools:targetApi="31">

        <activity
            android:name=".ui.project.ProjectDetailSub"
            android:exported="true" />
        <activity
            android:name=".codemanager.CodeDetailUpdate"
            android:exported="true" />
        <activity
            android:name=".codemanager.CodeDetailCreate"
            android:exported="true" />
        <activity
            android:name=".model.dto.auth.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".codemanager.CodeRecyclerView"
            android:exported="true" />
        <activity
            android:name=".ui.project.ProjectDetail"
            android:exported="true" />
        <activity
            android:name=".ui.project.ProjectCreate"
            android:exported="true" />
        <activity
            android:name=".codemanager.CodeDetailRecyclerView"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>